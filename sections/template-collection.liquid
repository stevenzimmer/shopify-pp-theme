<div class="container relative pb-20 pt-4">
  <div class="mb-6 flex flex-wrap items-center justify-end flex-col-reverse lg:flex-row">
    <div class="mb-3 w-full lg:mb-0 lg:w-1/2">
      <h1 class="mb-3 text-4xl text-bp-teal-dark md:text-5xl">{{ section.settings.title }}</h1>
      <p class="text-lg font-semibold">
        {{ section.settings.paragraph }}
      </p>
    </div>
    <div class="w-full lg:w-1/2">
      <div>
        {% if section.settings.texture %}
          {% render 'product-media', media: section.settings.texture %}

        {% else %}
          <img
            src="https://cdn.shopify.com/s/files/1/0671/5975/3003/files/collection-hero.png?v=1711489588"
            alt="{{ section.settings.title }} texutre"
            class="w-full"
            width="390"
            height="130"
          >
        {% endif %}
      </div>
    </div>
  </div>
  <div class="flex flex-wrap -mx-3">
     
    {% for product in collection.products %}
      <div class="w-full sm:w-1/2 lg:w-1/3 px-3 mb-6">
      {% render "product-card" | product: product %}
      </div>
    {% endfor %}
  
  </div>
</div>

{% schema %}
{
  "name": "Collection",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Shop CGM Cover Patches"
    },
    {
      "type": "image_picker",
      "id": "texture",
      "label": "Texture"
    },
    {
      "type": "text",
      "id": "paragraph",
      "label": "Paragraph",
      "default": "For Dexcom, Freestyle Libre, Medatronic Guardian, and Omnipod Monitors"
    }
  ]
}
{% endschema %}
