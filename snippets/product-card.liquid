<div class=" group relative overflow-hidden rounded-xl border-4 border-pp-grey-3 bg-white p-2 transition-shadow duration-200 hover:shadow-lg">
  <a href="{{ product.url  }}" class="absolute inset-0 z-20 h-full w-full">&nbsp;</a>
  <div class="relative rounded-lg bg-pp-grey-1 py-3">
    {% if product.featured_image %}
      <img
        width="{{ product.featured_image.width }}"
        height="{{ product.featured_image.height }}"
        src="{{ product.featured_image.src | img_url: 'large' }}"
        alt="{{ product.featured_image.alt | escape }}"
      >
    {% else %}
      {{ 'product-1' | placeholder_svg_tag: 'w-full h-full object-cover' }}
    {% endif %}

    <div
      class="absolute inset-0 flex h-full w-full items-center justify-center "
    >
      {% if product.price < product.compare_at_price %}
        <div class="absolute left-3 top-3">
          <div class="h-full w-full bg-pp-green px-2 py-1 text-sm font-semibold text-white">
            Sale
          </div>
        </div>
      {% endif %}
    </div>
  </div>
  <div class="p-3">
    <h3 class=" text-center text-xl  font-semibold text-pp-heading">
      {{ product.title }}
    </h3>
  </div>
</div>
